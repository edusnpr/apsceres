<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ceres">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ceres">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MenuActivity"/>
        <activity android:name=".LoginActivity"/>
        <activity android:name=".NewUsuario"/>

        <activity android:name=".AlagoasActivity"/>
        <activity android:name=".BahiaActivity"/>
        <activity android:name=".CearaActivity"/>

        <activity android:name=".RestauranteActivity"/>
        <activity android:name=".RestaurantePortoActivity"/>
        <activity android:name=".RestauranteCozinhaActivity"/>

        <activity android:name=".SururuActivity"/>
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>